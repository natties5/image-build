# Edit Guest Access — Implementation Summary
Date: 2026-03-22T18:13:00+07:00
Branch: fix/fresh-clone-and-paths

## Changes Made
- scripts/control.sh: added option 3 Edit Guest Access to menu_settings()
- scripts/control.sh: implemented _settings_edit_guest_access()
- scripts/control.sh: updated _settings_show() with Guest Access section
- settings/guest-access.env.template: updated with full schema (added SSH_CONNECT_TIMEOUT)
- settings/guest-keys/: created with .gitkeep
- .gitignore: added guest-keys/*.pem, guest-keys/*.rsa, guest-keys/id_* patterns

## Test Results
| Test | Description                    | Result    |
|------|--------------------------------|-----------|
| 1    | guest-keys directory           | PASS      |
| 2    | template has all fields        | PASS      |
| 3    | menu shows Edit Guest Access   | PASS      |
| 4    | password mode save/validate    | PASS      |
| 5    | Show Settings has Guest Access | PASS      |
| 6    | guest-access.env written       | PASS      |
| 7    | ShellCheck                     | N/A (not installed on Windows Git Bash) |

## Sample guest-access.env output
```
# settings/guest-access.env — generated by control.sh
# This file is gitignored — do not commit

SSH_AUTH_MODE="password"
SSH_USER="root"
SSH_PORT="22"
SSH_CONNECT_TIMEOUT="60"
ROOT_PASSWORD="***"
SSH_PRIVATE_KEY=""
ROOT_AUTHORIZED_KEY=""
ENABLE_ROOT_SSH="yes"
SSH_PERMIT_ROOT_LOGIN="yes"
SSH_PASSWORD_AUTH="yes"
SSH_PUBKEY_AUTH="yes"
```

## Notes
- shellcheck not available on Windows Git Bash; run on Linux to verify
- guest-access.env (gitignored) was pre-existing with old variable names (GUEST_AUTH_MODE etc.);
  new schema uses SSH_AUTH_MODE, SSH_USER, SSH_PORT, ROOT_PASSWORD
- Key selection scans settings/guest-keys/ for id_*, *.pem, *.rsa patterns
- Password input uses `read -rs` (silent, no echo)
- Validation warns if password empty (password mode) or key file missing (key mode)
