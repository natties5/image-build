# Change Log — 28/03/2026 ครั้งที่ 05

## สรุปงาน
OpenStack Pipeline 14 fixes — openrc session, create_vm overhaul, keypair UX

## สิ่งที่ทำสำเร็จ

### Fix Group A — openrc Single Source of Truth (5 phases)
- [x] ลบ hardcode `openrc-nutpri.sh` จากทุก phase
- [x] เพิ่ม `_load_active_openrc()` อ่าน `runtime/session/active-profile.env`
- [x] fail ชัดเจนถ้าไม่มี session หรือ AUTH_STATUS ไม่ใช่ "ok"

### Fix Group B — create_vm.sh (8 จุด)
- [x] B1: FLAVOR_ID/NETWORK_ID — ลบ fallback hardcode, ใช้ `:?`
- [x] B2: password — ใช้ ROOT_PASSWORD จาก guest-access.env
- [x] B3: cloud-init — format ใหม่ `type: text`, ลบ sed sshd_config จาก runcmd
- [x] B4: source guest-access.env ก่อนใช้ ROOT_PASSWORD
- [x] B5: trap cleanup USERDATA_FILE tmpfile
- [x] B6: conditional --key-name ใน server create
- [x] B7: Floating IP logic (FLOATING_NETWORK / EXISTING_FLOATING_IP)
- [x] B8: SSH wait ใช้ ROOT_PASSWORD และ SSH_PORT จาก guest-access.env

### Fix C — import_base.sh
- [x] เพิ่ม source openstack.env

### Fix D — publish_final.sh
- [x] source guest config ก่อน auto-promote → GUEST_OS_NAME ถูกต้อง

### Fix E — lib/config_store.sh (2 จุด)
- [x] E1: git status --porcelain แทน git diff --quiet (เห็น untracked file)
- [x] E2: git commit inline identity (-c user.email -c user.name)

### Fix F — scripts/control.sh (2 จุด UX)
- [x] F1: scan guest-keys ไม่แสดง .pub, auto-read .pub → ROOT_AUTHORIZED_KEY
- [x] F2: preflight warn เมื่อ SSH_AUTH_MODE=key แต่ ROOT_AUTHORIZED_KEY ว่าง

## ผลทดสอบ
| การทดสอบ | ผลลัพธ์ |
|----------|----------|
| bash -n ทุกไฟล์ | ✅ Pass |
| no active-profile → fail ชัดเจน | ✅ Pass |
| FLAVOR_ID missing → fail ชัดเจน | ✅ Pass |
| auto-read .pub | ✅ Pass |
| .pub filter จาก list | ✅ Pass |
| git status --porcelain untracked | ✅ Pass |
| git commit inline identity | ✅ Pass |
| dry-run ubuntu | ✅ Pass |
| dry-run debian | ✅ Pass |
| dry-run fedora | ✅ Pass |
| dry-run almalinux | ✅ Pass |
| dry-run rocky | ✅ Pass |
| dry-run alpine | ✅ Pass |
| dry-run arch | ✅ Pass |

## ไฟล์ที่แก้ไข
- phases/import_base.sh
- phases/create_vm.sh
- phases/configure_guest.sh
- phases/clean_guest.sh
- phases/publish_final.sh
- lib/config_store.sh
- scripts/control.sh

## Known Limitation (ยังไม่แก้)
- Alpine Phase 12: systemctl restart sshd ไม่มีบน Alpine → dropin เขียนสำเร็จ
  แต่ sshd ไม่ reload runtime — pickup ตอน reboot ซึ่งเกิดระหว่าง pipeline อยู่แล้ว
