# Change Log — 28/03/2026 ครั้งที่ 01

## สรุปงาน
แก้ไข configure_guest.sh และ clean_guest.sh — เพิ่มเงื่อนไข backup/vault เฉพาะเมื่อ official repo ล้มเหลว + รองรับ apk/pacman

## สิ่งที่ทำสำเร็จ
- [x] แก้ configure_guest.sh Phase 3: เพิ่มรองรับ apk/pacman
- [x] แก้ configure_guest.sh Phase 4: เพิ่มเงื่อนไข `if [[ "$_OFFICIAL_DEGRADED" == "true" ]]` ให้ backup เฉพาะเมื่อ official repo ล้มเหลว + รองรับ apk/pacman
- [x] แก้ configure_guest.sh Phase 5 Step 2: เพิ่มรองรับ apk/pacman (vault inject/validation/rollback)
- [x] แก้ configure_guest.sh Phase 5 Step 3: เพิ่มรองรับ apk/pacman (official last resort)
- [x] แก้ clean_guest.sh: เพิ่มการลบ backup directory `/var/backups/image-build/repos` ก่อน shutdown

## ข้อผิดพลาดที่เจอ
ไม่มี

## ผลทดสอบ
| การทดสอบ | ผลลัพธ์ |
|----------|----------|
| Bash syntax configure_guest.sh | ✅ Pass |
| Bash syntax clean_guest.sh | ✅ Pass |

## ไฟล์ที่แก้ไข
- phases/configure_guest.sh
- phases/clean_guest.sh

## หมายเหตุ
- Phase 4 (Backup): ทำเฉพาะเมื่อ official repo ล้มเหลว ไม่ทำทุกครั้ง
- Phase 5 (Vault Fallback): ทำเฉพาะเมื่อ official repo ล้มเหลว ไม่ทำทุกครั้ง
- รองรับทุก OS: apt, dnf-repo, apk, pacman
- Clean phase: ลบ backup directory ก่อน capture image
- APT cache เก็บไว้ให้ user ไม่ต้องโหลดใหม่
