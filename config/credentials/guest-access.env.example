# Guest bootstrap access credentials
# Copy to config/credentials/guest-access.env and fill in real values.
# This file is tracked in git (example only). guest-access.env is gitignored.

ROOT_USER=root
ROOT_PASSWORD=
SSH_PORT=22
ROOT_AUTHORIZED_KEY=
