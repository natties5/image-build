# Guest bootstrap config for create-vm stage
ROOT_USER="root"
# Keep empty in tracked config. Set locally in deploy/local/guest-access.env
ROOT_PASSWORD=""
SSH_PORT="22"

# optional authorized key to append to root at first boot
ROOT_AUTHORIZED_KEY=""
