# Jump host connection settings
# Copy to deploy/local/control.env and set real values locally.

JUMP_HOST_ADDR=
JUMP_HOST_USER=
JUMP_HOST_PORT=22
JUMP_HOST_REPO_PATH=
JUMP_HOST_BRANCH=
JUMP_SSH_KEY_FILE=
JUMP_SSH_CONFIG_FILE=
